def twoSum(numbers, target)
    x = 0
    y = 1
    result = []
    isSuccess = False
    while not isComplete:
        if y == len(numbers):
            
        if x + y = target:
            result = [x, y]
        else:
            y += 1
        
    return result